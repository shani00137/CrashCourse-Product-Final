VITE_API_URL=https://development.crashcourseonlin.net
# Local PaddleOCR (Flask) service the browser calls directly. Change if the
# OCR service is hosted elsewhere or behind HTTPS.
VITE_OCR_URL=http://localhost:5100/ocr
